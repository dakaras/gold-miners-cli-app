#!/usr/bin/env ruby
require "bundler/setup"
require "gold_miners"

GoldMiners::CLI.new.call

# this is the executable Ruby file that allows the program to be called from the terminal
# shebang line tells this file what program interpreter to run with for the user
